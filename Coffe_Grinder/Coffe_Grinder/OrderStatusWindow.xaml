<Window x:Class="Coffe_Grinder.OrderStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon ="Coffe_Grinder.ico"
        Title="Update Order Status" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Update Order Status" FontSize="16" FontWeight="Bold"/>

        <Label Grid.Row="1" Content="New Status:" Margin="0,10,0,5"/>
        <ComboBox Grid.Row="2" x:Name="StatusComboBox" DisplayMemberPath="StatusName"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Update" Click="Update_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>